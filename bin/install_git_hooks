#!/usr/bin/env bash
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel 2>/dev/null || pwd)"
cd "$repo_root"

git config --local core.hooksPath .githooks

echo "Configured Git hooks for ${repo_root}"
echo "Git will now run .githooks/pre-push before every push."
