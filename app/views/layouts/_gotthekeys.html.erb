<%= marketing_wordmark_tag(
  class_name: local_assigns.fetch(:class_name, "marketing-wordmark--inline"),
  alt: local_assigns[:alt],
  decorative: local_assigns.fetch(:decorative, false),
  variant: local_assigns.fetch(:variant, :default),
  width: local_assigns.fetch(:width, 180)
) %>
