<% title t("ui.properties.editor.new_title") %>

<section class="page-hero">
  <div>
    <p class="page-hero__eyebrow"><%= t("ui.properties.editor.eyebrow") %></p>
    <h1><%= t("ui.properties.editor.new_heading") %></h1>
    <p><%= t("ui.properties.editor.new_body") %></p>
  </div>
</section>

<section class="page-section">
  <article class="property-panel">
    <%= render "form" %>
  </article>
</section>
