<% title "#{t('properties.edit_property_details_for')} #{@property.address_line_1}" %>

<section class="page-hero">
  <div>
    <p class="page-hero__eyebrow">Seller tools</p>
    <h1>Edit <%= @property.address_line_1 %></h1>
    <p>Update the listing details, positioning, and metadata shown in the modern catalogue.</p>
  </div>
</section>

<section class="page-section">
  <article class="property-panel">
    <%= render "form" %>
  </article>
</section>
