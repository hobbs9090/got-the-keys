<% title "#{t('properties.edit_property_details_for')} #{@property.address_line_1}" %>

<section class="page-hero">
  <div>
    <p class="page-hero__eyebrow"><%= t("ui.properties.editor.eyebrow") %></p>
    <h1><%= t("ui.properties.editor.edit_heading", address: @property.address_line_1) %></h1>
    <p><%= t("ui.properties.editor.edit_body") %></p>
  </div>
</section>

<section class="page-section">
  <article class="property-panel">
    <%= render "form", show_seller_workspace_panel: true %>
  </article>
</section>
