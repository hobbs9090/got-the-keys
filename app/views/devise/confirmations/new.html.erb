<% title t(:resend_confirmation_instructions) %>

<div class="row">

  <!-- Resend confirmation instructions -->

  <div class="large-9 columns">

    <h4><%= t('devise.views.confirmations.new.title') %></h4>

    <%= form_for(resource, :as => resource_name, :url => confirmation_path(resource_name), :html => { :method => :post, data: { turbo: false } }) do |f| %>
        <%= devise_error_messages! %>
        <fieldset>
          <ol>
            <li class="required">
              <%= f.label :email, nil, **form_label_options(resource, :email) %>
              <%= f.email_field :email, autofocus: true, required: true, autocomplete: "email", **form_control_options(resource, :email) %>
              <%= field_error_messages(resource, :email) %>
            </li>
          </ol>
          <p>
            <%= f.submit t('devise.views.confirmations.new.submit') %>
          </p>
        </fieldset>
    <% end %>

    <%= render :partial => "devise/shared/links" %>

  </div>

  <!-- End Resend confirmation instructions -->

  <%= render "layouts/sidebar" %>

</div>
